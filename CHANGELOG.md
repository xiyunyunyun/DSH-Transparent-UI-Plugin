# Changelog

## Unreleased

### v1.5.0

- **主题更名 Seaglass（仓库 + 包名全链路）**：`@deepseek-ai/dsh-client-ui-aqua` → `@deepseek-ai/dsh-client-ui-seaglass`，仓库目录改名 `dsh-client-ui-seaglass`；同步更新 package.json（name/description/repository URL）、tsdown 预设的 ModuleLoader id 与 CSS 注入标签前缀、theme-layer 的 token override 来源标识、invariant 伴生插件的 PACKAGE_NAME/companion 名、cordis.patch.yml 的 roster 条目（id `ui-aqua` → `ui-seaglass`）、profile 依赖 link 与 bundles 清单、node_modules junction，以及双语 README 的安装/使用说明。内部 CSS 接缝（`data-dsh-aqua*` 属性与变量）、locale key、localStorage 键保持不变——它们是不可见的内部契约，重命名只会制造无意义的巨量 diff 与回归风险（用户旋钮设置也因此无损保留）
- **代码块玻璃化**：多行代码块（shell + banner 头部条）与单行行内代码此前的 token override 是纯不透明色（暗 #0D141F/#121B29/#172334），在满屏玻璃里读作塑料实板；三个 markdown token 改为随磨砂度滑杆联动的 color-mix 半透明值（块 55%、banner 50%、行内 62% × frost），外壳的 backdrop blur 保留——代码块成为与气泡/侧栏同语言的磨砂玻璃（code-block shell 本就是唯一绘制层，pre/bannerWrap 已被扁平化规则压透明，token 半透明化直接生效）
- **删除设置 → 插件页的冗余主题开关卡**：AquaPluginCard（标题 + 描述 + 一个总开关）与 Seaglass 设置页顶部的总开关完全重复，整卡移除（组件、模块 CSS、`settings.plugin.item` slot 注册、插件 store 镜像一并清理）；总开关只保留在 **设置 → Seaglass** 页顶部
- **设置导航「Aqua 主题」更名「Seaglass 主题」**（zh），英文「Aqua」→「Seaglass」
- 已验证：刷新后新包名下插件正常加载、设置页无插件卡片、导航显示 Seaglass 主题、代码块/行内代码 computed 背景为半透明 color-mix 且随磨砂度联动、主题开关与全部旋钮正常、0 控制台错误

### v1.4.7

- **主输入栏指令（命令选项）列表获得玻璃效果**：列表的可见面其实是 Radix 弹层的**壳**（`*_menu` 包装 div，position:absolute、涂着不透明 `--dsw-alias-bg-layer-2`），role=listbox 的视口嵌在壳内——主题的 listbox 玻璃规则一直命中视口，却被不透明壳整个盖住，视觉上仍是一块深色实板。seam stamper 现在给每个**锚定弹层**（position 非 fixed）的可见弹层的外壳打 `data-dsh-popover-shell` 标记（壳必须真的涂色——透明的定位包装器跳过；壳本身是 tilt pane 或 header/inputbar/轨迹/侧栏的跳过），样式表把壳变成玻璃（磨砂度滑杆联动）而壳内 role=listbox/menu/dialog 停止二次涂色——命令列表读作一整块悬于页面之上的磨砂玻璃，圆角/边框/阴影保持 stock
- **顶栏「轨迹」「上下文」页（及任何插件视图页）玻璃化**：上下文页是 dsh-context 插件挂在 `[data-slot="conversation.view"]` 的整页视图（`lc-root`），十块 `lc-card` 全是不透明 `--dsw-alias-bg-layer-1`；轨迹页的 split/table/plot/搜索同样用 layer token 涂成深色实板。seam stamper 给 conversation.view slot 下的每个非聊天根打 `data-dsh-view`（聊天视图由自身的 conversation.chat / tool.call 节点 slot 识别并跳过——它的专用接缝不动；内嵌 composer 的视图如 hero 页同样跳过），样式表把 layer-1/2/3/overlay 四个共享 token 在视图内改为随磨砂度滑杆联动的半透明玻璃值——**插件用共享 token 画的任何表面（现存的与未来的）自动变玻璃**，零协调；视图内 card 家族表面补 backdrop blur（stock 细线边框与圆角保留），内联 token 写死的表面用 !important 扁平化（同 5e 模式）。轨迹根 `[data-dsh-trajectory]` 并入同一 token 覆盖
- **视图矩形小面板获得倾斜/辉光效果（插件自适应）**：stamper 把插件视图内的 card 家族表面打上 `data-dsh-aqua-spot`（嵌套在已打标卡片内的跳过；无任何 card 的插件页回退为整页根一个 spot）——上下文页的十块统计/浏览器/趋势卡与未来插件的卡片页与侧栏/顶栏/输入栏一样按压倾斜 + 指针辉光；bubble-anchor 的钉位面板集合同步泛化为**全部 tilt spot**（新 spot 的 tooltip 自动获得逐帧钉位）
- **悬停打开的命令列表不再丢失倾斜**：输入栏的悬停几何（visualRect / glassLocalRect）并入已挂载的**锚定**弹层（fixed 弹层除外——它们本就由 keeper 滑回 tilt），指针从输入框移到列表上时不再因越出卡片盒而整体平放，列表随倾斜玻璃一致运动
- **半幅倾斜按尺寸泛化**：原先只对轨迹根减半的 tilt 幅度改为按 pane 最小边 >480px 判定（轨迹板、整页插件视图自动减半，卡片尺寸的 spot 全幅），轨迹行为不变
- 已验证：指令列表壳玻璃（computed 背景 = color-mix 半透明 + blur 20px、内部视口透明）、上下文页 10 张卡转 spot 且悬停倾斜、轨迹 split/table 半透明、聊天流零影响、悬停期间 scrollHeight 恒定、0 控制台错误

### v1.4.6

- **点击输入栏按钮不再丢失倾斜效果**：现版 DSH 的输入栏弹层（模型/访问模式/指令菜单、上下文面板）全部是 `position: absolute` 挂在 pane 内部、锚定触发元素——随倾斜玻璃一致运动，不存在视口重锚污染；tilt 阻断判定改为只针对真正 `position: fixed` 的可见弹层（对未来弹层保留防御），keeper 滑回同样只对 fixed 弹层生效。点击模型菜单、上下文面板实测：菜单/面板正常打开、tilt 保持、关闭后一切恢复，0 控制台错误

### v1.4.5

- **修复悬停输入栏按钮（发送/停止、上下文等）时的提示错位 + 滚动条跳动 + 玻璃突兀复位**：根因是 Host 的 Fd 气泡在挂载时先写入视口坐标、再测量渲染盒做视口夹取——tilt 的 transform 使输入栏成为气泡的包含块，测到的是「栏原点 + 视口坐标」的假渲染盒（x≈1839/y≈1856），夹取按假盒把 left 拉左 271px（innerWidth 1690），错位由此固化；隐藏气泡的占位盒还把聊天滚动区撑高，出现右侧纵向滚动条；而旧机制的滑回/瞬放就是「倾斜突兀复位」本身。新增 `bubble-anchor.ts`：悬停倾斜面板期间逐帧把气泡钉回「触发元素中心 + 8px 间距 + 12px 视口夹取」（校正走独立的 translate 属性，应用的 inline left/top 与箭头 transform 不动）——tooltip 挂载即显示在正确位置，输入栏全程保持倾斜（校验：tilt 关闭时 left=1380.19 正确；tilt 开启时被夹取为 1108.92，修复后钉回 1380.19）
- **触发元素识别按 DOM 序定位（v1.4.5 二次修复）**：Fd 渲染顺序保证气泡的前一个元素兄弟就是触发元素；初版用「父内唯一非气泡子元素」判定，在触发元素的父容器带其它兄弟时误判歧义而放弃钉位——「发送消息/停止」的气泡挂在含多个按钮的 trailing 行、「上下文已用」气泡的容器本身干净、统计行的 dock 里还有隐藏跳转锚点，于是恰恰是最常用的两个按钮完全不被修正（错位 + 滚动条依旧）。改为 prev-sibling 定位（跳过杂散 tooltip 兄弟）+ 保留单子元素扫描兜底
- **挂载即同步钉位**：MutationObserver 微任务先于浏览器绘制——气泡的首个渲染帧就是正确位置，被污染的帧不再进入绘制，幻影滚动区从未产生
- **transform 写入点的同任务重钉（v1.4.5 三次修复，滚动条单次跳动）**：tooltip 在 bar 平放时挂载（夹取正确、无需修正）、随后同一帧内 tilt 写入 transform 的时序下，气泡被重新锚进栏坐标系——等下一帧 rAF 再补钉会放过恰好一帧被污染的布局进绘制，表现为滚动条滑块缩一下又弹回（「下方似乎多了内容可以滚动但实则没有」）。spotlight.ts 现在在每个 transform 写入/移除点（tilt 写入、瞬时释放、缓回、keeper 滑回及其清理、侧栏快放）同任务调用 `repinPanelBubbles()`——任何时序下被污染的布局都不再有机会进入绘制（逐帧采样验证：3 次挂载/卸载循环 scrollHeight 恒定、0 尖峰）
- **倾斜面板滚动溢出kill switch（v1.4.5 四次修复，悬停即跳且持续）**：用户实测悬停输入栏（无需悬停按钮）滚动条滑块立即向上跳一次并**持续整个悬停期**，滚动文本后消失——这是任意 `position:fixed` 后代（tooltip、隐藏弹层等）在 pane 携带 transform 期间被重锚进 pane 坐标系、其视口坐标盒落在视口下方数百 px、**持续**撑大祖先滚动区的结果；逐帧补钉只保证可见位置，无法约束未钉元素。现给全部倾斜面板加 `overflow: clip; overflow-clip-margin: 64px`：pane 的可滚动溢出被上限裁剪在自己的盒外 64px——钉住的气泡（≤36px 外）照常可见，被污染的数百 px 盒子不再计入任何祖先滚动区；平放时 fixed 后代的包含块是视口、天然逃逸裁剪，完全不受影响；菜单/对话框/listbox 可见期间由 seam stamper 的 `data-dsh-popover-live` 标记豁免（keeper 本就会为它们滑回 tilt）。实测：伪造隐藏弹层 + tilt 下 scrollHeight 纹丝不动，tooltip 照常显示于按钮正上方
- **钉位改写 inline left/top、废弃 translate（v1.4.5 五次修复，剩余小幅跳动）**：裁剪上线后仍残留 +51px（7091→7142）——判定实验证实 `translate` 只移动绘制盒，**布局盒**仍停留在被污染的坐标上、被裁剪边缘计入滚动区（+51 = 栏底 + 64 裁剪余量 − 12px 内边距）。钉位改为直接改写 inline `left/top`（delta 法：每帧量「渲染 − 意图」差值写回，自动吸收包含块偏移，平放/倾斜同样收敛）——布局盒本身落到栏内/栏上方，滚动区增量归零。实测：空闲会话悬停切换 4 次 scrollHeight 恒定 7091（修复前 7091↔7142 跳变），上下文/发送按钮 tooltip 均精确居中（1380=1380、1424=1424、8px 间距），0 控制台错误
- **token 栏（统计行）提示回到正上方、不再被侧边栏遮盖**：同一 Fd 夹取机制，756px 宽气泡的假渲染盒更宽、被拉左更多（视觉上"跑到左上方"）；bubble-anchor 按统计行自身几何钉位，天然跟随倾斜玻璃
- **对话框/菜单保持原滑回机制**：它们靠测量渲染盒自夹取、无法逐帧重钉，仍由 keeper 滑回 transform 后再显示；tooltip 从该机制中豁免，不再触发滑回
- **修复点击各种按钮时的背景流体卡顿**：性能追踪实锤两类主线程放大器——
  - 流体着色器渲染循环每帧读 `canvas.clientWidth/Height`（30fps × 每次点击后的 React 提交都会使布局失效 → 循环内强制整页 reflow）；mesh 同理。尺寸同步改为 ResizeObserver + window resize 事件，渲染循环零布局读取；mousemove 的画布矩形改用缓存
  - seam-stamper 与 overlay-keeper 的 MutationObserver 回调按「每批变更」同步跑十几个 :has 查询 + 属性写入 + 强制布局，放大了 Host 自身的重排成本；统一合并为每帧一次
  - 鲸鱼引擎每帧 positionHost（querySelector + gBCR + 样式写入）改为事件驱动（phase 容器 ResizeObserver + 重挂载监视 + 250ms 节流）；pointermove 的容器矩形改用缓存
- **构建**：补齐缺失的 `tsconfig.build.json`（rolldown 1.2.7 无法解析旧 AquaControls 的嵌套三元展开，重构为显式 push）；tsdown 输出扩展名钉回 `.js`（0.22 默认 CJS 输出 .cjs）；上一轮只在 lib/client.js 里手工追加、未进源码的旧"气泡补偿器"由 `bubble-anchor.ts` 正式取代（旧版本信任被夹取污染的 inline left，修正量恒为 0，无法自愈）

### v1.4.4

- **适配 DSH 0.1.2-rc.1**：弃用已移除的 `@deepseek-ai/dsh-client-runtime` 客户端模块——
  - 设置行 store（`settings-store.ts`）改从 `@deepseek-ai/dsh-client-store` 导入 `defineStore`/`EngineStoreHandle`（0.1.2-rc.1 把 client-runtime 的 store 能力拆分为独立模块，前端模块表已注册 `dsh-client-store`、不再提供 `dsh-client-runtime`）
  - 客户端插件上下文从 `ClientContext` 改为普通 cordis `Context`（`src/client/index.ts`）
  - `settings.register()` 直接接收命名空间字符串：0.1.2-rc.1 的 dsh-settings 移除了 `settingsNamespace` 辅助函数，注册接口内联校验命名空间
  - peer 依赖整体升至 `^0.1.2-rc.1`，`@deepseek-ai/dsh-client-store` 以 optional peer 声明；devDependencies 脱离 workspace 协议（`tsdown` 本地构建）
  - `cordis.patch.yml` 的插件名改为 scoped 包名 `@deepseek-ai/dsh-client-ui-aqua`

### v1.4.3

- **修复设置页/用量账本打开时面板区域明暗度闪烁**：删除 `[role=dialog]::before` 的 backdrop-filter 模糊层——`isolation: isolate` 使对话框成为自己的 backdrop root，该伪元素只能采样到面板自身的半透明底色（永远采不到面板后的页面），实际效果是把底色模糊着再叠一遍，且挂载首帧背板捕获内容不一致，产生「先很暗 → 回落 → 保持」的跳变；面板背后的磨砂本来就由 Host 自带的弹窗遮罩（55% 填充 + blur 2px）提供，主题只保留干净的半透明底色即可
- **面板底色回调加浓**：删除双重叠层后面板偏透，暗色 60%→72%、浅色 65%→75%，观感贴近改动前的有效浓度
- **插件面板玻璃重构（账本获得与设置页同款磨砂）**：去掉上一版误加的 body 级整页底幕——用户要的是面板自身的「当前页面模糊+深色」，而非整页 UI 被模糊；非 Host 对话框（VOzbGW_ 前缀之外）改为 `isolation: auto` + 面板底色透明 + `::before` 承载 blur(2px) 与两层遮罩色扁平化的单一填充（暗 `rgba(13,20,30,.874)` / 浅 `rgba(185,187,191,.888)`，数学上等价 Host 遮罩55%+面板72% 的叠加），面板区域观感与设置面板完全一致、面板外 UI 不受影响。`isolation: isolate` 会把对话框变成自己的 backdrop root、导致模糊只能采样自身（反色滤镜实验实锤），故必须摘除；同时删除对话框入场动画——opacity<1 的淡入过程本身会临时成为 backdrop root，让磨砂在淡入结束时"啪"地出现
- **用量账本「账户/今日/本月/累计」刷新闪烁修复**：对话框内控件（stat/selector/select/tkl_select/badge/chip/pill/tag）统一去掉 backdrop-filter——隔离面板内的 blur 只会采样面板自身填充，毫无磨砂收益，却在账本每次数据刷新重渲染时反复重捕获导致框框闪烁；改为固定玻璃填充（暗 `rgba(30,44,64,.62)` / 浅 `rgba(255,255,255,.62)` + 内高光），不再随磨砂度滑杆降到近透明
- **设置页黑色背景元素玻璃化 + 插件窗口通用适配**：设置页 5 个 `selector` 下拉按钮从不透明 `#111A27` 改为玻璃芯片；对话框内所有卡片（`card`/`Card` 通配，覆盖插件卡/市场卡及未来新插件）从实色 `#1C2A3D` 改为 45% 玻璃板——只填色不加描边：通配规则会命中卡片内部件与下拉选项卡，画上去的边框线读起来像成排的杂线（stock 表面本就无边框），实测确认后移除；卡片规则同时排除列表容器（`:not(ul):not([class*='cards'])`），否则容器染色会让卡片之间的间隙不再透明；Aqua 主题页的分段激活项、数字输入框、字体选择按钮、滑杆轨道、开关开启态玻璃化；插件用内联 `--dsw-alias-bg-layer-*` token 写死的面板（如 ModLens 配置提示框）以 `!important` 强制玻璃化
- **侧边栏按钮气泡防裁断/防闪烁（保留倾斜）**：气泡（`[role=tooltip]`）是视口锚定的，而倾斜机构写入的 transform——哪怕是恒等矩阵——都会让侧边栏成为气泡的包含块，气泡被再锚定 ~13px 且遭 wrapper 链 overflow:hidden 逐层裁剪。修复分两半：CSS 在侧边栏含气泡期间对包含气泡的任意包装层（泛化 `:has`，未来按钮自动覆盖）放行 `overflow: visible`；client.js 新增气泡锚定补偿器——pointerover 即时武装 rAF 逐帧闭环（测量「渲染位置 − 期望位置」误差并写回气泡独立的 `translate` 属性，与应用的箭头 transform 自动叠加），并把气泡 `transition` 置 none——应用的 `transition: all` 会把每次逐帧修正重定目标成过渡动画，正是之前闪烁错位的来源；倾斜全程保留
- **统计行悬浮气泡防裁断/防滚动条跳动**：输入栏统计行（`data-dsh-stats`）纳入 `overTrigger` 豁免选择器——悬停统计行时输入栏平放（与输入栏按钮既有行为一致），其 756px 宽的详情气泡保持视口锚定；此前倾斜激活时输入栏成为气泡包含块，气泡（右锚定）整体左移 ~249px、左缘落在主栏边界被裁（视觉上像被左侧边栏裁断），且气泡局部坐标超出滚动容器底部、计入 scrollHeight 导致右侧滚动条拇指跳变
- **气泡文字按明暗模式配色**：应用给气泡写死浅色文字，浅色模式下浅底浅字不可阅读；`[role=tooltip]` 现按模式设置文字色（浅色模式深字 / 暗色模式浅字）
- **会话悬浮详情卡玻璃化**：侧边栏会话行悬浮弹出的完整标题+状态卡（`_card_1b2ny`，Host CSS-module 哈希类）stock 为不透明灰板，接入玻璃（blur 14 + 半透明填充 + 描边），文字按明暗模式适配

### v1.4.2

- **倾倒触发器豁免收窄至输入栏**：`overTrigger` 的 snapOff 作用域从「全 UI 所有无 `title` 按钮」收窄为「仅输入栏（`data-dsh-inputbar`）内的按钮」——侧边栏「设置/搜索/展开其余会话」与上边栏按钮悬停时不再把所在玻璃面板突兀复位定住，面板保持倾倒跟随；输入栏内发送/停止/命令等按钮仍保留 snapOff（其自定义 tooltip 是视口锚定的，面板带 transform 会错位，需摘除后才能对齐）

### v1.4.1

- **Agent 任务列表玻璃元素适配**：todo 工具胶囊（`[data-tool=todo_write]`）与任务列表面板（`[data-testid=todo-panel]`）接入玻璃配方——半透明填充 + backdrop blur + 内高光，明暗两套成对适配，任务列表不再是一块实色面板
- **对话框玻璃重构**：设置面板/插件弹窗从本体 `backdrop-filter` 改为 `isolation: isolate` + `::before` 承载模糊（`z-index:-1`）——消除 backdrop-filter 把弹窗内 fixed 定位后代（如用量账本热力图 tooltip）重新锚进弹窗坐标系的问题，模糊效果不变
- **新建会话按钮 hover 玻璃高亮**：悬停时从纯白调高亮改为蓝色玻璃（`#6e9be8` 18% 填充 + 蓝色描边 + 外辉光），明暗两套
- **倾倒交互对触发器豁免**：鼠标悬停在按钮/menu item/option 等交互元素上时立即取消倾倒（snapOff 无过渡移除 transform），避免倾倒把触发器自身的 tooltip/菜单错误锚进玻璃片坐标系；倾倒会话期间只隐藏 tooltip，不再强制隐藏 dialog/menu/listbox

### v1.4.0

- **Aqua 独立设置页**：主题设置从通用设置迁出，注册为独立 section（导航顺序：通用 → Aqua 主题 → 模型 → 插件）；页面顶部自带主开关——即使 Host 未加载插件 Host 半的部署里也能直接开关主题；插件页卡片注册保留，两处状态自动同步
- **中英文字体分别自定义**：新增「字体」分组，英文字体（默认 Space Grotesk）与中文字体（默认苹方/雅黑系统栈）独立选择
  - 自绘玻璃下拉菜单：原生 `<select>` 弹层无法定制主题且暗色下闪白，改为触发按钮 + 自绘玻璃列表（blur/圆角/边框/内高光，明暗两套），展开方向按面板可视空间自适应，支持键盘导航（Enter/Space/↑↓/Esc）、外点关闭、当前项高亮
  - 系统字体枚举：Local Font Access API（首次展开请求授权，拒绝后回落内置常用列表）
  - 中文优先分组：中文字体列表「系统中文」置顶、拉丁字体沉底；英文字体列表相反——用映射表 + canvas 探测（「永語字」测量宽度比对）分类
  - 双语显示：40 余项常见 CJK 字体「中文名（English）」映射（queryLocalFonts 只报英文名）；存储值始终为英文 family 名，CSS 引用跨平台可靠
  - 代码块跟随：`--ds-font-family-code` 覆盖为 `--dsh-aqua-font-mono`（用户拉丁+中文字体+monospace 兜底），仅在至少设置一项字体时写入——未设置时代码保持原等宽栈
- **主输入栏倾倒效果恢复**：与顶栏/侧边栏一致的鼠标位置驱动倾倒（rotateX/rotateY + scale 1.01）
  - 根因：倾倒的 transform 会把 bar 内 `position:fixed` 弹层（发送/停止 tooltip、统计详情、模型菜单）错误锚定进 bar，此前被整体禁用
  - 方案：可见性三态状态机——倾倒会话中弹层隐藏（错位帧不绘制）→ keeper 0.12s 平滑收回 transform（错位弹层位于视口外坐标，双重不可见）→ 收回完成同帧放行弹层并重播淡入（位置精确）→ 鼠标离开后常驻统计提示隐藏、倾倒立即可恢复
- **统计提示不再被侧边栏覆盖**：提示挂载期间输入栏层级抬升到侧边栏之上（`:has` 驱动，设置面板打开时自动撤回避免反盖）
- **玻璃化补全与调透**：
  - 代码块（外壳+头部条）磨砂化：token 半透明化（`calc(基线 + 系数×磨砂度)` 调制，实测磨砂 0→50% / 7→55% / 50→88%）+ 外壳 backdrop blur；清掉 DSL 内层（pre/bannerWrap）的叠加实色（原双层叠加 ≈82% 实色且滑杆无效）；行内小代码片同样磨砂度调制
  - 设置面板玻璃化：60%/65% 半透明 + 模糊度可调 + 内高光（此前 85% 实色无模糊）
  - 菜单/下拉/tooltip 调透至 65%/70% 并补齐 listbox/tooltip 缺失的 blur
  - 原生 `<select>` 兜底：第三方插件对话框里的 select 本体玻璃化、弹层配色锁定（暗色不再闪白）
- **Aqua 设置页内嵌**：自绘下拉的 `zoom` 等比缩放中间方案被倾倒需求替代，未随版本发布

## 旧候选（未随 v1.4.0 发布，已并入上文）

- 修复侧边栏会话搜索框聚焦时出现意外蓝色边框：全局键盘焦点环豁免侧边栏搜索输入框（`[data-dsh-sidebar-root] input:focus-visible`），其焦点已由展开胶囊边框与光标表达
- 适配第三方插件（如「用量账本」dsh-tokenledger）：侧边栏徽章默认透明、悬停浅灰蓝，与「设置」按钮一致
- 修复悬浮小窗口被大窗口截断：侧边栏 / 主框架含弹窗时解除裁剪（`overflow: visible` + `transform: none`）
- 权限模式 / 模型选择等弹出菜单改为玻璃背景，前后景文字不再混杂
- 面向未来插件：任意插件弹窗内的统计按钮 / 下拉框 / 徽章 / 标签等常见语义元素自动玻璃化

## v1.3.1

- 适配 DSH 0.1.1-rc.1：插件设置卡片使用 `settings.aqua` keyed slot，不再向 `settings.plugin.item` 传递旧的 `id`。

## v1.3.0

- 视频壁纸全面升级：浏览器原生解码、无进度条、循环播放、铺满裁剪，窗口缩放实时自适应
- 视频壁纸「记住文件授权」：选一次即可（File System Access，刷新自动读取原文件、改动即时生效、不占额外存储；非 Chromium 自动退回数据库存储）
- 视频壁纸自动柔光罩 + 聊天气泡实底，保证文字在视频上清晰可读
- 视频壁纸新增「模糊度」「亮度」两个滑杆（含提醒文案）
- 发送栏与数据栏共用一块玻璃：单一背景/模糊/边框/投影，分界线精确落在左右圆角弯曲处，两片玻璃不再出现模糊度/清晰度不一致
- 数据栏文字、发送栏占位符改中性色（深色模式近白、浅色模式深灰），去除蓝色调
- 玻璃卡片平板化：去掉卡片底部黑色渐变，视觉更干净

## v1.2.1

- 鼠标辉光颜色跟随「色调」设置（与流体晕染色共用色相基准），不再默认泛蓝

## v1.2.0

- 新增「悬停下压」：官网卡片同款 3D 倾斜（哪边鼠标哪边下压、透视 800px + 微放大 1.01、0.1s 过渡），顶栏/侧边栏/发送框+数据栏/轨迹窗统一配方，轨迹窗幅度减半，设置里可开关
- 新增「鼠标辉光」：跟随光标的荧光青光晕，位于玻璃之下从磨砂中透出，不遮内容
- 设置页玻璃化：50px 磨砂、深浅色区分、兼容模式生效；打开设置即时弹出不再卡顿
- 设置分组优化：环境装饰（粒子鲸鱼/小鱼/网状交互）与悬停效果（鼠标辉光/悬停下压）分组，子标题放大加粗
- 背景新增「色调」「颜色深浅」双滑条：全程无极调节；0°/360° 锚定蓝色、默认 320° 青蓝；深浅 0 端直落纯黑（深色）/深浓原色（浅色），100 端浅淡粉彩
- 兼容模式修复：注入层不再顶歪顶栏/新建会话按钮/发送栏
- 浅色模式聊天文字微投影 + 1px 光晕
- 出厂默认：模糊 20 / 磨砂 7 / 色调 320 / 深浅 25 / 亮度 50，全部开关默认开启

## v1.1.2

- 新增「小鱼」开关（背景游动的小鱼/气泡，位于粒子鲸鱼开关右侧）
- 壁纸新增「删除」按钮（清除已选壁纸）

## v1.1.1

- 侧边栏收起状态悬浮玻璃，收起带渐隐动画
- 页面顶部/底部渐变模糊带（浅色泛白、深色泛黑）
- 漂浮玻璃投影加深
- 新建会话按钮透明玻璃，磨砂度跟随磨砂旋钮 +20
- 统计行与发送框分界线更清晰
- 设置页不再被顶栏遮挡；展开时 Harness 铭牌实时就位
- 设置文案简化

## v1.1.0

- 新增「粒子鲸鱼」：deepseek.com/harness 同款（官网粒子引擎 2D 移植），显示在聊天主区域正中央（不含侧边栏），深色模式白粒子、浅色模式灰粒子，设置里有开关可关
- 深色模式侧边栏铭牌换成官网同款「Harness」光泽药丸（135° 渐变描边 + 柔光 + 黑底白字），浅色模式保持原版铭牌
- 页面顶部/底部新增 5px 渐变模糊带，悬浮在聊天内容上层，内容滚到边缘渐入模糊；浅色模式微泛白、深色模式微泛黑
- 「漂浮玻璃」更名「云母效果」

## v1.0.8

- 通用设置里的玻璃主题控件去掉独立标题，直接跟在「外观」下方；插件页总开关关闭时，通用设置里的整块调节自动隐藏，打开开关即恢复
- 「选择图片」旁新增提示：浅色壁纸用浅色模式，深色壁纸用深色模式⚠️
- 设置排版统一：壁纸行改为「标签 + 控件」结构，模式/亮度/壁纸三条说明文字全部与所解释的控件列对齐

## v1.0.7

- 背景亮度自动跟随深浅模式：**深色模式只能在 0–50 调节**（压暗，50 原样），**浅色模式只能在 50–100 调节**（提亮，50 原样）；「跟随系统」按实际解析出的深浅色自动切换，旋钮范围与提示文字实时翻转，另一侧的旧值自动失效不干扰

## v1.0.6

- 设置重新布局：总开关留在「插件」页（卡片形状与其他插件一致），其余全部调节（模式/模糊度/磨砂度/流体颜色/背景亮度/背景/壁纸）移到**通用设置 → 外观**的正下方；主题未开启时该行会变暗并提示开关位置

## v1.0.5

- 修复「背景亮度」0–50 无效的问题：黑色遮罩原来画在流体/壁纸**下面**被盖住，现与白色遮罩合并到同一顶层（`::after`），0 = 死黑、100 = 死白、50 = 透明全部生效

## v1.0.4

- 新增「背景亮度」调节（0 = 纯黑、50 = 透明、100 = 纯白），漂浮/兼容两种模式都可用

## v1.0.3

- 安装器改为**优先 zip 下载**（普通 HTTP 更快更稳），git clone 只作后备——git 连 GitHub 不稳时不再白等十几秒

## v1.0.2

- 安装脚本默认安装**最新发布版**（通过 GitHub API 解析，可 `-Version 'v1.0.1'` 指定版本或 `-Version 'main'` 跟随开发分支）
- 修复安装脚本在 Windows PowerShell 下中文提示乱码（UTF-8 BOM）

## v1.0.1

- 兼容模式新增「背景流体颜色」调节入口（颜色旋钮在两种模式下都可用）
- 介绍补充兼容模式（双模式）说明

## v1.0.0

首个正式版本：可开关的玻璃主题，双模式 + 高自由度自定义。

- **漂浮玻璃模式**：顶栏、侧边栏、发送框、统计行、轨迹视图变成悬浮玻璃卡片，聊天区在流体上滑动
- **兼容模式**：保持原版排版一字不动，只把材质换成通用玻璃——按角色和类名子串命中菜单/卡片/气泡/下拉/提示等保守表面，其他插件的界面同样自动玻璃化
- **可调旋钮**（无极滑块 + 数值框）：玻璃模糊度、磨砂度、背景流体颜色
- **背景**：流体板或自定义壁纸（铺满页面、比例不变），壁纸单独可调模糊度/磨砂度，图片自动压缩后本地保存
- 深色玻璃改中性冷灰，磨砂度拉满不再泛蓝；发送栏正确模糊背后的内容
- 问候语等文案功能独立，不在本主题内
- 一键安装 `install.ps1`（按版本号安装，无 git 时自动退回 zip 下载）
